{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "live",
      "type": "shell",
      "command": "start http://localhost:8000 && uv run livereload --host localhost --port 8000 docs",
      "icon": {
        "id": "run"
      }
    }
  ]
}
