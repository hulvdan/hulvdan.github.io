{
  "runOnSave.commands": [
    {
      "match": ".(json|css)$",
      "command": "prettier -w ${file}"
    },
    {
      "match": ".(md|css|html)$",
      "command": "uv run python build.py"
    },
    {
      "match": ".pyi?$",
      "command": "uv run ruff format ${file} && uv run ruff check --select I ${file} --fix"
    }
  ]
}
